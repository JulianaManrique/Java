window.onload = function(suma) {
    const denominador = div=> document.getElementById(div);
    const calcula = () => ;




    let numeradorUno =  nomDiv("numeradorUno").value;
    let denominadorUno = nomDiv("denominadorUno").value;
    let numeradorDos = nomDiv("numeradorDos").value;
    let denominadorDos = nomDiv("denominadorDos").value;
    if (numerador.length === 0, estatura.length === 0) {
        alert("por favor complete los campos");
    } else {
        const denominador = denominadorUno * denominadorDos;
        const numerodorOpera
        const numeradorCorrecto = numerodorOperativo + numeradorUno;
        
    } 









